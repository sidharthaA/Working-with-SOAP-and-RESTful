you can run the files in folder micTesting> my-app> src> App.js by
npm run start

The app runs on 
http://localhost:3000 

to test rest api's please copy the scipt saved in REACToutput in app.js in src folder
and run the following in terminal:

npm i axios --save
npm start 

The submitted file is incomplete and has errors when tried to itegrate it with SOAP API.
